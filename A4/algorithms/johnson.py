"""
    johnson.py
    ~~~~~~~~~~~~
    Contains Johnson algorithm for computing the all-pairs shortest paths of a graph.
"""

from heap import Heap

def johnson(self):
    reweighted_graph = self.reweight()
    # for node in self.nodes(reweighted_graph.
    pass
